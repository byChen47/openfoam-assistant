# Development-only inputs: the corpus, tooling and review artefacts.
# The published extension needs only src/, data/keywords/, syntaxes/ and the
# metadata files, so everything else is excluded from the VSIX.
OpenFOAM-v2606/**
data/source-supplements/**
tools/**
tests/**
# Development workflow notes for agents, not part of the published extension.
Agent.md
# Source image for the icon generated by tools/build-vsix.ps1.
OpenFOAM.ico
.gitignore
.vscodeignore
.git/**
.vscode/**
**/*.map
*.vsix
package-lock.json
